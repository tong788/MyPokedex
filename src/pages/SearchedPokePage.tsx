import Layout from "../components/Layout";
import SearchPokemon from "../components/SearchedPokemon";

const SearchedPokePage = () => {
  return (
    <Layout>
      <SearchPokemon />
    </Layout>
  );
};

export default SearchedPokePage;
